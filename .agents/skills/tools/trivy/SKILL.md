---
name: trivy
description: Trivy-scan a filesystem, image, dependency set, secret surface, or IaC configuration.
---

Choose only the scanners that match the artifact, save structured output, and compare configuration candidates with the deployed path where available. Pass every material candidate to `/verify-security-finding`.

Complete when every material result names the affected artifact and its verification disposition.
