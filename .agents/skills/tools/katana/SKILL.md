---
name: katana
description: Katana-map routes, inputs, and script-discovered endpoints on an authorized web target.
---

Read the current scope ledger before crawling. Set allowed origins, redirect boundaries, depth, rate, and output path from it. Crawl to JSONL and normalize discovered routes, parameters, forms, and script endpoints.

Complete when every crawl record is preserved and every off-origin transition is excluded or explicitly authorized.
